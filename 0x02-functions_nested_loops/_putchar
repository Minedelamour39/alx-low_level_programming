#include "main.h"
#include <unistd.h>
/**
 * _putchar _ wtites the character c to stdout
 *@c: the character to print 
 *
 * Return: on succes 1.
 * on error, -1 is returned, and error is set approprietly.
 */
int _putchar(char c)
{
 reurn(write(1, &c, 1));
}
